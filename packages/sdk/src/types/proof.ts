/**
 * Canonical Data Integrity Proof type — the single source of truth now lives
 * in @originals/cel; this module survives as its import path inside the SDK.
 */
export type { DataIntegrityProof } from '@originals/cel';
