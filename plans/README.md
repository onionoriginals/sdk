# Implementation Plans

Generated by the `improve` skill on 2026-06-11, planned against commit `879ab0c`.
Each plan is self-contained and written for an executor with no prior context —
read the plan fully before starting, honor its STOP conditions, and update your
row when done.

These came out of a full-repo audit of `packages/sdk` (+ `packages/auth` surface)
across all nine categories. The headline finding (001) is a **proven credential
forgery** in the legacy verification path.

## Execution order & status

| Plan | Title | Priority | Effort | Risk | Depends on | Status |
|------|-------|----------|--------|------|------------|--------|
| 001 | Bind credential verification to issuer DID (close forgery) | P1 | M | MED | — | DONE |
| 002 | Stop logging private keys in CEL CLI | P1 | S | LOW | — | DONE |
| 003 | Make `tsc` pass — fix 12 build errors | P1 | S–M | LOW | — | DONE |
| 004 | AuditLogger Ed25519 signing + re-enable in MigrationManager | P1 | M | MED | — | DONE |
| 005 | Implement `rotateDIDWebVHKeys` / `recoverDIDWebVH` | P2 | L | HIGH | — | DONE |
| 006 | Extract shared credential digest helper | P2 | M | MED | 001 | DONE |
| 007 | Cache the DocumentLoader per factory | P2 | S | LOW | — | TODO |
| 008 | DIDCache O(1) LRU + EventEmitter alloc | P3 | S | LOW | — | TODO |
| 009 | Exclude examples/playground from published build | P3 | S | LOW | 003 | TODO |
| 010 | Gate CI on typecheck + test exit code | P1* | S | LOW/MED | 003, 004, 005 | TODO |
| 011 | Reconcile README/example lifecycle API drift | P3 | M | LOW | — | TODO |
| 012 | Decompose LifecycleManager god module | P3 | L | HIGH | 003, 010 | TODO |
| 013 | (SPIKE) Unified `verify()` entry point | P3 | M | LOW | 001 | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (one-line reason) | REJECTED (one-line rationale)

\* 010 is conceptually P1 (it's the gate that lets everything else rot) but is
ordered last because turning it on while the repo is red just makes CI red. Land
the fix plans first.

## Recommended waves

- **Wave 1 (urgent):** 001 (security), 002 (security), 003 (build). Independent;
  can run in parallel by different executors. 001 and 003 both touch the VC layer
  — land 001 first to avoid a conflicting edit in `CredentialManager.ts`.
- **Wave 2 (correctness/features behind the failing tests):** 004, 005. These are
  the substantive items inside the ~43 failing tests.
- **Wave 3 (quality):** 006, 007, 008, 009, 011.
- **Wave 4 (keystone + big rocks):** 010 (only once 1–9 make the suite green),
  then 012 (large refactor, protected by 010's gate), and 013 (spike).

## Dependency notes

- **006 depends on 001** — plan 001 edits `CredentialManager`'s digest/verify
  (adds `delete proofSansValue.publicKeyMultibase`); 006 extracts that exact
  logic into a shared helper. Doing 006 first would force a re-edit.
- **009 depends on 003** — you can't confirm "the real build still works after
  excluding demos" until the build compiles.
- **010 depends on 003 + 004 + 005** (and any other failing-test fix) — the gate
  can only be turned on once `tsc` and `bun test` are green locally; otherwise CI
  goes red immediately.
- **012 depends on 003 + 010** — a 2178-line refactor needs a compiling codebase
  and a real CI gate protecting it.
- **013 depends on 001** — a unified verifier must inherit the issuer-bound
  verification, not the forgeable legacy path.

## Findings considered and rejected

So these are not re-audited next run:

- **CEL guide `eddsa-jcs-2022`** (reported as a docs bug): rejected — CEL
  legitimately uses `eddsa-jcs-2022`; only the VC subsystem uses
  `eddsa-rdfc-2022`. Not a bug.
- **Auth package rate-limiting / DDoS** (`packages/auth` Turnkey client):
  rejected as a plan — impact depends entirely on unknown endpoint exposure;
  LOW confidence, not actionable without the deployment context. Revisit if/when
  the auth server's routes are in scope.
- **Regtest→testnet address-validation fallback** (`utils/bitcoin-address.ts`):
  rejected — by design and documented; validation still occurs.
- **MultiSigManager weaker key binding** (`extractPublicKeyFromVM`): not rejected,
  but intentionally deferred. It derives the key from a `did:key:` identifier
  (self-certifying) and checks policy membership, so it is not the same trivial
  bypass as 001. Folded into 001's "Maintenance notes" as a follow-up rather than
  its own plan.

## Notes on scope of the audit

Audited at `standard` depth: whole `packages/sdk` tree, CI config, build/test
health, plus the surface of `packages/auth`. Lighter coverage: CEL subsystem
internals, Bitcoin PSBT construction math, and the Turnkey auth flows in
`packages/auth` — none of these were deeply read, so a future `deep` pass there
may surface more.
