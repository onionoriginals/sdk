# SDK 3.0.0 candidate verification

Current candidate runtime: `b82aae0e`. The completed release review and bounded
follow-up close all nine actionable code findings. This record is release
preparation evidence, not authorization to deploy or publish.

## Verified local gates

- Frozen lockfile install; package build, source typecheck, lint and Railway IaC
  typecheck pass. Existing lint warnings remain; no lint errors.
- Full package suites: 4,632 pass, 248 explicitly skipped, zero failures. Counts
  include retained previous-format regression suites and are not a claim that
  all those tests prove CEL 3 publication.
- Landing CI command: 1,021 pass, no failures; frontend/server typechecks and Vite
  build pass. Tests run in their documented unset-network fixture environment;
  a separate production bundle is built with the mainnet/canonical-domain flags.
- Node 20.10 imports all 24 entry points checked by the repository ESM gate;
  browser static-import guard passes. Public types compile against built exports,
  including negative assertions for the retired API.
  The gate preserves existing workspace links and installed packages, including
  when imports fail. Six Node CLI regressions cover cleanup. A fresh Bun 1.2.22
  install reproduces CI's hoisted layout and passes ESM followed by public types.
- Isolated stable preview produces SDK 3.0.0, CEL 1.0.0, auth 3.0.0 and private
  landing 0.2.0. Real npm tarballs install into a fresh consumer using local
  overrides for the unpublished stable dependencies. Packed entry imports,
  binary creation, concurrent edits, cold recovery, private-path denial,
  TypeScript contracts and CLI version all pass. Licenses and current package
  guides are present. Nothing was published.
- Real Core 31.1 / ord 0.29.0: normal delivery, reveal rejection with closed-browser
  sweep, and accepted commit with lost response all pass. Every mode covers full
  PNG boundary, exact stored pair, restart, boundary/rotation reorgs, six-confirmation
  retention, controller rotation, actual sat sale/reacquisition, resource delta,
  log-only deactivation and fresh DID/resource reads.
- Mainnet-format PNG build/sign fixture passes with zero broadcast-route calls.
  Rebased production-source Chrome journey passes local HTTPS PNG create/publish/
  reload/cold resolution, same DID and exact bytes, no page errors or Bitcoin calls.

## Original eight reproduced defects

| Defect | Replacement public regression seam |
| --- | --- |
| Anonymous upload replaces app JavaScript | `apps/landing/server/tests/upload-isolation.test.ts`: trusted namespaces remain immutable to anonymous upload |
| Genesis legacy fields bypass identity binding | `packages/cel/tests/v3/profile.test.ts`, `history.test.ts`; `CelV3DefaultJourney.test.ts`: strict native identity/profile and refusal of previous envelopes |
| Commit response loss loses reveal bytes | `inscriptionRecovery.test.ts`; all real regtest fault receipts: durable exact pair before first send, restart without signing |
| First confirmation retires reorg recovery | landing `inscribe-routes.test.ts`; real boundary/rotation reorgs: one through five retain; six retires |
| Signed keys omitted/numeric keys misordered | CEL `values.test.ts`, `known-answers.test.ts`: own prototype-like keys, lexical canonicalization and independent known answers |
| Inline-media asset has no derived DID | SDK `v3/resolution.test.ts`; real PNG DID/resource cold reads agree on the accepted head |
| Unproven resource versions verify | SDK `v3/validation.test.ts`: substituted/unproven/missing historical bytes fail full verification |
| Concurrent appends lose successful events | `CelV3DefaultJourney.test.ts`, `v3/asset.test.ts`: every successful concurrent edit survives serialized recovery |

## Release review

Completed `ce-code-review` run `20260906-090911-cc7e0c5f`: ten local lenses and
an independent Claude Opus 5 adversarial pass, followed by one validator batch.
Requested peer high effort could not be verified. Follow-up inspection and
regressions close all nine findings; no new concrete regression was found.

The fixes verify reveal witnesses before either SDK or HTTP submission, replay
both evicted transactions through automatic and manual recovery, meter costly
requests and bound complete snapshots to 30 seconds/512 requests by default.
They also retain signed retitles and explicit inline media, restore the hosted
evidence type, exclude genesis aliases from Bitcoin labels and discard stale
account recovery continuations. Three actual React/Chrome regressions cover
account changes and unmount; CI skips those three when Chrome is unavailable.

SDK coverage is 88.84% functions / 88.44% lines (80% required). Auth/account and
Bitcoin-spending changes remain material review surfaces even with passing
fixtures. Already initiated publication requests can finish after account change;
subsequent account writes and retained-artifact removal are guarded.

[Original review](evidence/release-review.json) and
[closure receipt](evidence/release-review-closure.json) preserve the findings.

## Existing follow-up triage

Recovery starvation and swallowed store writes were reproduced and fixed through
public poll/background seams. Five lookup slots reserve progress for every
nonempty category; store failures produce a named 503 and money log, and replay
attempts are persisted before sending. The four signer redirection/input attacks
also run in the SDK security suite.

The remaining structural-debt suggestions (extract component functions, split the
route factory, split identity UI modes, unify historical auth error shapes) do
not demonstrate an unresolved failure in this journey. They are deferred; this
release does not promise those refactors. Current named auth failures and custody
regressions remain tested.

## Evidence and outstanding acceptance

- [API freeze](3.0.0-public-api.md)
- [Package preview](evidence/stable-package-preview.json)
- [Regtest receipts](evidence/regtest-summary.json)
- [Zero-broadcast fixture](evidence/mainnet-format-png-dryrun.json)
- [Browser receipt](evidence/landing-cel3-browser/README.md)
- [Measured operator prerequisites and owner runbook](3.0.0-operator-preflight.md)

The authorized production preflight verified and configured byte-exact PNG
transport, enabled daily backups, and verified a native backup restore in an
isolated service. Its temporary resources were removed after verification.
The production provider now passes complete snapshots and exact PNG/metadata
byte checks. A real no-address-index transfer regression confirms ownership;
170 provider adapter tests pass. Candidate `e9c51870` is deployed and remote health/auth/network checks pass.
Live Turnkey OTP, owner signing, hosted PNG publication and fresh stable SDK
resource verification pass. Owner mainnet retry, public chain verification and
six confirmations remain outstanding. The final release verdict stays **not ready**
until the owner proof is complete. Production
preflight and deployment are authorized; package publication remains separate.

## Production provider correction verification

Runtime `71e57306` resolves the measured production snapshot failures; see
[causal evidence and review](evidence/provider-capability-fix.md). All required
GitHub checks pass: 4,651 package tests, 210 explicit skips, zero failures;
1,023 landing tests pass. SDK coverage remains 88.84% functions / 88.44% lines.
All three creator regtests and the new transfer without address indexing pass.
Fresh stable tarballs pass Node 20.10 imports for all 29 entry points, public
types, binary/concurrent/cold-load checks and CLI 3.0.0.

[CI checks](evidence/provider-fix-ci.json) · [Fresh package receipt](evidence/provider-fix-package-summary.json)

## Browser recovery correction verification

Runtime `e9c51870` replaces Node-specific Buffer comparisons with byte-array
comparison in recovery validation. The owner attempt failed before provider
submission; its signed pair remains retained for the owner to retry.

The browser Buffer regression reproduced the production error before the fix and
passes afterward. A real browser fixture accepts the valid pair and rejects
signature/control-parity tampering with zero real broadcasts. Package tests pass
(4,651 passed, 248 skipped, zero failed); 1,024 landing tests pass. Build, lint, ESM,
browser checks and real Core/ord regtests pass. Fresh stable packages pass Node
20.10 imports, strict public types, binary/concurrent/cold-load checks and CLI
3.0.0. Review completed with no actionable findings; required runtime CI passes.

The corrected production deployment is successful. The owner browser loads its
new bundle and exposes **Retry signed transactions**. Fresh SDK 3.0.0 verification
again resolves the hosted PNG with the exact 3,857-byte digest. Mainnet submission
and six-confirmation proof are still pending; the release is not ready yet.

[Regression and checks](evidence/browser-buffer-fix.json) ·
[Fresh packages](evidence/browser-buffer-package-summary.json) ·
[Review receipt](evidence/browser-buffer-review.json) ·
[Deployment and hosted checks](evidence/browser-buffer-deployment.json) ·
[Owner proof](evidence/owner-mainnet-proof.json)
