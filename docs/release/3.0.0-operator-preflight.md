# CEL 3 release operator preflight

The release candidate must pass the local verification receipt and code review
before the owner mainnet journey. The owner authorized production preflight and
deployment on 2026-09-06.
Package publication remains a separate final release action. The observations
and completed actions below distinguish preparation from live evidence.

## Final status — 2026-09-07 01:08 UTC

**SDK 3.0.0 is ready for release.** Owner proof: nine confirmations at tip 965862,
accepted controller history, byte-exact PNG, hosted metadata and separate
possession verified. Production recovery is confirmed and retired, with both
signed transactions removed after the six-confirmation horizon. The temporary
SSH inspection key was removed. No new transaction or npm publication occurred.

[Final chain verification](evidence/owner-mainnet-chain-final.json) ·
[Retirement receipt](evidence/owner-mainnet-recovery-final.json)

The observations below are chronological evidence; their intermediate pending
states are superseded by this final result. The hosted timeline display
inconsistency is a recorded follow-up and does not change SDK acceptance.

## Initial read-only production observations, 2026-09-06

The Railway production `builder` service currently serves commit
`9116ce2c364872f7993336050786e722da86c2e2` on `originals.build`. The release candidate
has not been deployed. `/data` is mounted on the existing 50 GB volume;
`NODE_ENV=production`, `BTC_NETWORK=mainnet`, `VITE_BTC_NETWORK=mainnet`,
`VITE_WEBVH_HOST=originals.build`, and `TRUSTED_PROXY_HOPS=1` are configured.
Required auth variable names are present; this does not verify live OTP signing.

The initial check identified three unresolved prerequisites:

1. The configured QuickNode status has `sat_index=true`,
   `inscription_index=true`, and `address_index=false`. The candidate initially required all three indexes. Subsequent
   pinned ord source review and a real transfer regression established that
   `/sat` ownership does not use the inverse address index; see the resolution below.
2. Neither raw binary content transport nor an explicit base64 gateway contract
   is configured. Supply `QUICKNODE_CONTENT_BASE_URL` (HTTPS ord-compatible raw
   `/content/:id`) or a verified `QUICKNODE_CONTENT_ENCODING=base64` gateway.
   Verify a known PNG's exact SHA-256 through the actual production provider.
3. The production volume API returns no backup schedules and no backup snapshots.
   Enable the agreed backup schedule, record its retention, and verify a backup
   and a restore into an isolated destination before relying on hosted durability.

Receipts: [production properties](evidence/production-preflight.json) and
[provider capabilities](evidence/production-provider-capabilities.json). Secrets
and provider URL values are deliberately absent. These observations can drift;
repeat them immediately before an approved deployment.

## Authorized preflight results, 2026-09-06

- **Raw PNG transport verified and configured.** The existing endpoint's
  `/addon/679/content/:id` route returned the same 793 PNG bytes as the original
  transaction witness for inscription zero, including when requested with the
  SDK's `Accept: application/octet-stream` header. SHA-256 is recorded in the
  [transport receipt](evidence/production-content-transport.json).
  `QUICKNODE_CONTENT_BASE_URL` now points at that verified add-on base; its secret
  value is omitted from evidence. Configuration was saved without triggering a
  deployment. The gateway labels the raw response `application/json`; the SDK
  reads bytes, and the witness comparison verifies their actual content.
- **Daily backups and an isolated native restore verified.** Daily retention is
  518,400 seconds (six days). The named pre-release backup was restored into a
  separate volume. Before committing the staged restore, its production mount
  swap was removed and the restored volume was attached only to a temporary,
  private maintenance service without app credentials or public domains.
  The verifier read 361 files (291,643 bytes), parsed all 50 JSON files, and
  recorded a deterministic file-manifest digest. Production retained its original
  deployment and volume throughout. The temporary service and restored copy were
  deleted; the original backup and daily schedule remain. See the
  [backup/restore receipt](evidence/production-backup-restore.json).
- **Complete production snapshots now pass.** The SDK no longer requires the
  inverse address-to-outputs index for `/sat`, which derives ownership from the
  current output. Sat/inscription indexes, explicit health and ownership fields,
  complete enumeration and stable Core/ord tips remain required. A real ord
  transfer with `address_index=false` preserved all publication bytes and reported
  the new owner/satpoint ([regtest receipt](evidence/regtest-no-address-index.json)).
  Regtest address-dependent operations retain their address-index guard.
  The configured raw gateway also serves metadata: 418 CBOR bytes matched the
  original witness ([metadata receipt](evidence/production-metadata-witness.json)).
  Only ord's exact inscription-specific 404 marker establishes absence; generic
  404s, server errors, success-null and decoded objects fail. The complete known-PNG
  snapshot and digest now pass through the real production provider
  ([provider preflight](evidence/production-provider-check.json)). The earlier
  [failed snapshot](evidence/production-snapshot-preflight.json) is retained as
  historical evidence. No QuickNode dashboard change is needed.
- **Initial release candidate deployed and remote checks passed.** Production initially ran
  `71e57306ed08aa50ab1954c459a0fa9b8631471d`, deployment
  `12730c33-396a-4ba4-8db3-622da700e392`, on the original `/data` volume.
  The existing `railway.json` build/start configuration was retained; the IaC
  preview proposed only two safe property updates and was not applied as an
  unrelated migration. Startup reports clean configuration, mainnet mode and
  enabled auth. Public health/network checks pass, protected routes return 401,
  and the Railway hostname redirects to `originals.build`.
  See [deployment](evidence/production-deployment.json) and
  [HTTP checks](evidence/production-http-checks.json).
- **Owner signing and hosted PNG proof pass; Bitcoin retry remains pending.**
  The owner signed in through live OTP and uploaded `tla-logo.png` (3,857 bytes).
  Browser creation/publication and a fresh stable SDK consumer verified both signed
  histories and every hosted resource. The browser PNG digest matches the hosted
  attachment exactly. The displayed 7,782-sat quote is covered by 17,434 sats
  already deposited; the owner-approved cap remains US$20 total.
  The owner's first inscription attempt failed at 22:00:54 UTC in the browser's
  pre-submission recovery validator: `Argument must be a Buffer`. Node accepted
  its Uint8Array comparisons, but the browser Buffer package rejected them.
  No provider submission was reached. The browser retains the exact signed pair
  and exposes **Retry signed transactions** in **Your Originals**. Resume that
  pair on the corrected deployment; do not create a replacement inscription.
  Fix `e9c51870` is now deployed successfully as
  `d3b2b97c-5305-47d7-a3df-5c7279b501b2`, preserving `/data`. Health, mainnet,
  unauthenticated route rejection and fresh stable SDK PNG verification pass.
  The signed-in owner browser loads the fixed bundle and shows the retained retry.
  All required runtime CI checks pass. See the
  [fix deployment receipt](evidence/browser-buffer-deployment.json).
  See [owner proof](evidence/owner-mainnet-proof.json),
  [fix verification](evidence/browser-buffer-fix.json), and
  [fresh package verification](evidence/browser-buffer-package-summary.json).
  Bitcoin submission, fresh public-host/browser chain verification and six
  confirmations remain pending. Package publication remains a separate action.

## Prepared change and verification order

1. Review the release PR, code review and API freeze. The feature PR exits
   prerelease mode but retains pending changesets. The existing Version Packages
   PR is the single npm publication approval: merging its stable version commit
   can publish SDK 3.0.0, CEL 1.0.0 and auth 3.0.0 to `latest`. Do not merge the
   earlier prerelease Version Packages proposal as the stable release.
2. Recheck the completed provider, transport and backup evidence for drift. Run the read-only
   `apps/landing/scripts/check-quicknode-ordinals.ts` with a confirmed outpoint,
   `BTC_CHECK_PNG_SAT`, and `BTC_CHECK_PNG_SHA256`. Require complete fresh sat
   evidence, exact bytes and current active-chain ordering. Missing configuration
   or an incomplete provider response is a failed gate.
3. Preview `.railway/railway.ts` against production. The recorded preview has
   only build/start/restart property changes and no deletions. It preserves the
   current Railpack builder, domain, volume, other services and existing operator
   values. Recheck the plan before applying; the saved preview is not approval.
4. Under the recorded deployment approval, deploy the reviewed candidate with the canonical domain and
   mainnet flag baked into the SPA. Verify startup configuration, mounted storage,
   canonical redirect, client IP handling and a real Turnkey OTP session whose
   bound public key matches the browser key. Re-login must clear a stale bound key.
5. Perform the owner-funded PNG journey through the deployed browser: sign in,
   upload the owner's PNG (at most 32 KiB), create, publish, deposit confirmed BTC,
   inspect the actual full-envelope quote, and submit the persisted signed pair.
   Record quoted fee, maximum approved spend, funding txid, commit/reveal txids,
   inscription id, sat, did:cel, did:webvh and did:btco. A transport timeout is an
   ambiguous result; retry the same prepared pair from recovery.
6. Close the browser and verify the Original from a fresh browser with no local
   state. Require exact PNG bytes/digest, accepted CEL head, derived DID controller,
   separate possession observation and confirmation evidence. Verify the earlier
   WebVH identity still resolves and its PNG remains byte-identical. Record any
   UI defect before changing the candidate; changed code needs renewed evidence.
7. Verify background reconciliation and retained recovery through the configured
   six-confirmation horizon. Monitor named `inscribe_failed` persistence errors,
   stale recovery records and provider incompleteness. Storage errors must surface
   as failure, never as successful durable recovery.

The owner mainnet transcript is the remaining completion evidence for #525,
#527 and #540. Local regtest, fixture signing and localhost browser receipts do
not close those tickets or prove live Turnkey operation. If deployment checks
fail, keep the exact signed pair and inspect its fresh chain status before any
rollback or replacement; application rollback cannot undo Bitcoin transactions.

## Owner retry reached mainnet

At 2026-09-07 00:32 UTC both the commit and reveal were independently observed in
the mempool, with zero confirmations at tip 965853. The owner retried the retained
pair through the fixed browser. Originals displays `did:btco:321959825743820` and
its reveal transaction as awaiting confirmation. Actual network fees total
3,850 sats (442 commit + 3,408 reveal), with a 546-sat inscription output.

The fresh stable SDK correctly finds no confirmed boundary yet. Continue with
steps 6 and 7 after confirmation/indexing; do not retry or fund a new pair. A
15-minute task heartbeat continues the authorized release checks and pauses once
release readiness is established. This is broadcast evidence, not finality.

[Broadcast receipt](evidence/owner-mainnet-broadcast.json) ·
[Unconfirmed fresh resolution](evidence/owner-mainnet-unconfirmed-resolution.json)

The initial server retry at 00:28:57 UTC persisted the pair and reported
`commit_broadcast` after QuickNode rejected the reveal with
`bad-txns-inputs-missingorspent`. The identical reveal was subsequently observed
in mempool.space. This establishes live broadcast despite the earlier provider
error; final durable server reconciliation must still be checked.
[Sanitized server events](evidence/owner-mainnet-server-broadcast.json).

## Confirmed owner proof before the retention horizon

Fresh observations at 00:47–00:50 UTC establish reveal inclusion in active block
965854, initially three and then four confirmations. A fresh Node 20.10 consumer
of the stable SDK 3.0.0 tarball accepts all three controller events and the exact
3,857-byte PNG. The derived controller matches the live signer; the separate
possession observation is the owner's address at reveal output 0, offset 0.

The inscription carries the PNG, while `metadata.json` remains a hosted resource.
Chain-only resolution accurately reports that missing attachment. Resolving the
earlier WebVH identity, attaching its independently verified metadata, and
reloading against fresh sat evidence produces `verification.verified: true`.
The WebVH PNG remains byte-identical.

A reopened browser page independently verifies the hosted artifacts and reports
`confirmed on-chain`. Its hosted provenance timeline still describes the Bitcoin
step as Up next; this display inconsistency is recorded separately from the
correct confirmed badge and SDK chain result. The browser does not itself display
the accepted three-event chain in that hosted timeline.

Read-only production file inspection confirms durable status `confirmed`, with
both signed transactions retained and `retired: false` before six confirmations.
The temporary Railway SSH key used for inspection was removed afterward. Verify
retirement after the horizon before declaring release readiness.

[Accepted chain and complete asset](evidence/owner-mainnet-chain-verification.json) ·
[Confirmation observations](evidence/owner-mainnet-confirmations.json) ·
[Durable recovery before horizon](evidence/owner-mainnet-recovery-before-horizon.json)
