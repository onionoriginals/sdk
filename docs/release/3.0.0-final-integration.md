# Final pre-release integration

The combined candidate includes #577 (Demo logic extraction), #578 (published
inscription timeline), #581 (public Explore) and #582 (Changesets v2 workflow
correction). Integration began at `b917f1fa`; the reviewed merge was `8f117dfb`.
Follow-up `8366e167` closes the discovered issue and adds credential observations.
No SDK package source changed in this integration.

## Standards

No actionable documented-standard violations in the combined diff. Review
covered public discovery ownership and signature bindings, controller versus
holder semantics, hash-checked image blobs, preserved eager engine loading and
the upstream Changesets action contract.

## Spec

One P2 finding was reproduced and fixed: a malformed percent-encoded candidate
DID made account listing throw, hiding the account's other valid publications.
Parsing now isolates that candidate. A real signed-publication regression fails
before the fix and passes afterward. No remaining spec findings.

The added credential observer received a separate review. Its ISO expectation
was corrected; token metadata is accepted only from a complete response with
one masked match. Incomplete and ambiguous responses remain unconfirmed. All
seven workflow/credential tests pass, and actionlint passes the changed workflows.

## Validation

- Combined landing: 1,032 tests, zero failures; frontend/server typechecks and
  production build pass.
- Full package suites, package typechecks and lint pass (existing warnings).
- Twenty-four package entry points import; browser-facing export checks pass.
- Real-SDK Chromium gallery/detail checks pass with 26 signed publications,
  pagination/search, image/text previews, tampered bytes, retry, empty/missing
  states and 320/375/768px layouts. Screenshots were visually reviewed.
- All three real Core 31.1 / ord 0.29.0 journeys pass: normal, reveal rejection,
  and commit accepted with its response lost. These are disposable regtest
  chains, not additional mainnet transactions.

[Regtest checks](evidence/final-integration-regtest.json) ·
[Combined CI](https://github.com/onionoriginals/sdk/actions/runs/34079718288)

## npm credential

The actual stored GitHub `NPM_TOKEN` authenticated to npm. A complete token-list
response uniquely matched its masked value and reports package-write permission
for `@originals`, 2FA bypass enabled and expiry **2026-10-25T06:53:28.982Z**.
The non-secret expiry variable is now recorded as `2026-10-25`. No rotation is
needed on this evidence. The separate account-package endpoint returned 403;
configured token permissions are verified, not a successful publish operation.

[Sanitized credential receipt](evidence/final-npm-credential.json) ·
[Read-only GitHub check](https://github.com/onionoriginals/sdk/actions/runs/34079718365)

## Release boundary

The earlier deployed owner mainnet proof remains the live runtime evidence.
This integration does not deploy the landing, broadcast transactions, merge to
main or publish packages. The stable Version Packages PR remains the final npm
publication approval. The browser was locked during the credential check;
runner-side metadata supplied the evidence without requiring local npm login.
