---
"@originals/sdk": patch
---

Bump `multiformats` from 12.1.3 to 14.0.2. No consumer-facing API changes; the SDK only relies on the stable `multiformats/bases/base58` and `multiformats/bases/base64` submodule entry points, which are unaffected by this major version bump.
