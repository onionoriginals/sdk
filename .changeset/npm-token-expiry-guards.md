---
---

CI and docs only: add an `npm whoami` preflight to `release.yml` and a weekly
`NPM_TOKEN` expiry guard workflow, and document both in the rotation runbook. No
published package changes, so this carries no version bump.
