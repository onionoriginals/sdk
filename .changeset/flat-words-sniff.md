---
---

Docs and release plumbing only — the 2.x → 3.0 migration guide, plus entering
changesets prerelease mode so 3.0 can go out under the `next` dist-tag for the
first implementers to build against. No package code changes.
