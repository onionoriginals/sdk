---
---

chore(deps): bump the dev-tools devDependency group in `packages/sdk` (Babel, `@types/node`, `typescript-eslint`, ESLint, TypeScript). Dev-tooling only — no published code changes. Migrated the SDK's ESLint config to flat config (`eslint.config.js`) for ESLint 9+/10 compatibility and fixed lint violations surfaced by the newer `typescript-eslint` rule set.
