---
---

deps(landing): upgrade the private `@originals/landing` app to React 19,
react-dom 19, Vite 8, @vitejs/plugin-react 6, TypeScript 7 and @types/react(-dom) 19.
No published `@originals/*` package changes, so this needs no release — empty
changeset satisfies the "Changeset present" gate.
