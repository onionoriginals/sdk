[test]
preload = ["./packages/sdk/tests/setup.bun.ts"]
coveragePathIgnorePatterns = [
  "tests/mocks/**",
  "tests/setup.bun.ts",
  "dist/**",
  "apps/**"
]

# Security scanning can be enabled by installing a scanner:
# bun add --dev snyk
# Then uncomment the following:
# [install.security]
# scanner = "snyk"